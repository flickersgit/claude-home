# Mochi Environment Variables
# Copy this to .env and fill in your values

# Your personal WhatsApp number — always allowed, required
# Format: use the @lid format discovered from debug logs (e.g. 156556037517475@lid)
# Or @c.us format: countrycode + number (no spaces, no +) e.g. 6580735469@c.us
OWNER_NUMBER=6580735469@c.us

# Optional: comma-separated extra numbers allowed to message Mochi
# ALLOWED_NUMBERS=6581234567@c.us,6589876543@c.us

# Absolute path to the game-arcade project
PROJECT_DIR=/Users/statpods/Documents/Claude Home

# Path to the claude CLI binary (defaults to /Users/statpods/.local/bin/claude)
# CLAUDE_PATH=/Users/statpods/.local/bin/claude

# Codex CLI path and model (execution phase)
# CODEX_PATH=/usr/local/bin/codex
# CODEX_MODEL=o4-mini

# Heartbeat daemon interval in milliseconds (default: 30 min, set to 0 to disable)
# HEARTBEAT_INTERVAL_MS=1800000

# Quiet hours — no alerts sent during this window (format: HH:MM-HH:MM, local time)
# HEARTBEAT_QUIET_HOURS=22:00-08:00
